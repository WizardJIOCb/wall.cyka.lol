# Исправление функции поиска и кнопки Create
**Дата:** 07 ноября 2025, 19:30  
**Задача:** Исправить позиционирование иконки поиска, API запросов поиска и функциональность кнопки Create

## Проблемы

### 1. Иконка поиска
- **Проблема:** Иконка лупы находилась посередине строки поиска, а не слева
- **Причина:** Неправильное позиционирование CSS для `.search-icon`

### 2. API запросов поиска  
- **Проблема:** Поиск возвращал 400 Bad Request
- **URL запроса:** `https://wall.cyka.lol/api/v1/search?params%5Bq%5D=FFdds&params%5Btype%5D=all...`
- **Причина:** Параметры передавались в вложенном формате `params[q]` вместо плоского `q`
- **Дополнительные проблемы:**
  - Использовался `DiscoverController::search()` вместо `SearchController::unifiedSearch()`
  - `SearchController` использовал несуществующий метод `Database::escape()`
  - Именованные параметры PDO (`:search`) не работали с LIMIT/OFFSET

### 3. Кнопка Create
- **Проблема:** Кнопка Create не работала
- **Причина:** Переход на `/ai` не был реализован, отсутствовал модальное окно создания поста

## Исправления

### 1. Исправлена иконка поиска в AppHeader.vue

**Файл:** `/frontend/src/components/layout/AppHeader.vue`

**Изменения:**
```vue
<!-- До -->
<div class="search-container">
  <input v-model="headerSearchQuery" ... />
  <button class="search-btn" @click="handleHeaderSearch">
    <span class="icon">🔍</span>
  </button>
</div>

<!-- После -->
<div class="search-container">
  <span class="search-icon">🔍</span>
  <input v-model="headerSearchQuery" ... />
</div>
```

**CSS:**
```css
.search-icon {
  position: absolute;
  left: var(--spacing-3);
  top: 50%;
  transform: translateY(-50%);
  font-size: 1.25rem;
  color: var(--text-primary);
  opacity: 0.5;
  pointer-events: none;
}

.search-input {
  width: 100%;
  padding: var(--spacing-2) var(--spacing-3);
  padding-left: 40px; /* Отступ для иконки */
  ...
}
```

### 2. Исправлен API маршрут поиска

**Файл:** `/public/api.php`

**До:**
```php
// Global search
route('GET', 'api/v1/search', function() {
    DiscoverController::search();
});
```

**После:**
```php
// Global search
route('GET', 'api/v1/search', function() {
    SearchController::unifiedSearch();
});

// Trending searches
route('GET', 'api/v1/search/trending', function() {
    SearchController::getTrendingSearches();
});
```

### 3. Исправлен SearchController

**Файл:** `/src/Controllers/SearchController.php`

**Проблемы:**
- Использование `Database::escape($query)` - метод не существует
- Именованные параметры PDO с LIMIT/OFFSET

**Решение:**
Заменены все запросы на использование позиционных параметров `?`:

```php
// До
$searchTerm = Database::escape($query);
$sql = "... AGAINST(:search ...) LIMIT :limit OFFSET :offset";
$results = Database::query($sql, [':search' => $searchTerm, ':limit' => $limit, ':offset' => $offset]);

// После  
$sql = "... AGAINST(? ...) LIMIT ? OFFSET ?";
$stmt = Database::query($sql, [$query, $query, $limit, $offset]);
$results = $stmt->fetchAll();
```

**Исправленные методы:**
- `searchPosts()` - поиск по постам
- `searchWalls()` - поиск по стенам
- `searchUsers()` - поиск по пользователям
- `searchAIApps()` - поиск по AI приложениям
- `getTrendingSearches()` - популярные запросы
- `trackSearch()` - логирование поисковых запросов

### 4. Добавлена функциональность кнопки Create

#### 4.1 Обновлен UI Store

**Файл:** `/frontend/src/stores/ui.ts`

**Добавлено:**
```typescript
const createPostModalOpen = ref(false)

const openCreatePostModal = () => {
  createPostModalOpen.value = true
}

const closeCreatePostModal = () => {
  createPostModalOpen.value = false
}
```

#### 4.2 Обновлен AppHeader

**Файл:** `/frontend/src/components/layout/AppHeader.vue`

**До:**
```typescript
const handleCreate = () => {
  router.push('/ai')
}
```

**После:**
```typescript
const handleCreate = () => {
  uiStore.openCreatePostModal()
}
```

#### 4.3 Добавлено глобальное модальное окно

**Файл:** `/frontend/src/layouts/DefaultLayout.vue`

**Добавлено:**
```vue
<Teleport to="body">
  <AppModal 
    v-model="uiStore.createPostModalOpen" 
    title="Create Post" 
    size="md"
    @close="uiStore.closeCreatePostModal"
  >
    <CreatePostForm @success="handlePostCreated" />
  </AppModal>
</Teleport>
```

#### 4.4 Создан компонент CreatePostForm

**Новый файл:** `/frontend/src/components/posts/CreatePostForm.vue`

**Функциональность:**
- Текстовое поле для контента (с подсчетом символов 0/5000)
- Опциональное поле для заголовка
- Загрузка медиа файлов (изображения/видео)
- Предварительный просмотр медиа
- Валидация размера файлов (макс 10MB)
- Автоматическое получение wall_id пользователя
- Загрузка через FormData с multipart/form-data
- Обработка ошибок и loading состояние
- Emit события `success` при успешном создании

**Основные возможности:**
```typescript
- formData: { title, content, visibility }
- mediaFiles: File[]
- handleFileSelect() - выбор медиа файлов
- removeMedia() - удаление медиа
- handleSubmit() - отправка поста на сервер
- getFilePreview() - предпросмотр файлов
```

## Итоги

### ✅ Что исправлено

1. **Иконка поиска** - теперь позиционирована слева в строке поиска
2. **API маршруты** - поиск использует правильный контроллер `SearchController::unifiedSearch()`
3. **SearchController** - все SQL запросы используют позиционные параметры
4. **Кнопка Create** - открывает модальное окно создания поста
5. **Создание постов** - полноценная форма с загрузкой медиа

### 📝 Технические детали

**Backend:**
- Замена именованных параметров на позиционные `?`
- Удаление несуществующего метода `Database::escape()`
- Правильная работа с `Database::query()` и `fetchAll()`

**Frontend:**
- Глобальное состояние модального окна через UI Store
- Teleport для рендеринга модального окна
- Компонент CreatePostForm с валидацией и загрузкой файлов

### 🚀 Результат

- Поиск работает корректно с параметрами `q`, `type`, `sort`, `limit`
- Иконка поиска на правильном месте
- Кнопка Create открывает форму создания поста
- Пользователи могут создавать посты с текстом, заголовком и медиа

### 📊 Измененные файлы

**Backend (PHP):**
1. `/public/api.php` - добавлен маршрут для SearchController
2. `/src/Controllers/SearchController.php` - исправлены все методы поиска

**Frontend (Vue/TypeScript):**
1. `/frontend/src/components/layout/AppHeader.vue` - исправлена иконка поиска и кнопка Create
2. `/frontend/src/stores/ui.ts` - добавлено состояние модального окна
3. `/frontend/src/layouts/DefaultLayout.vue` - добавлено глобальное модальное окно
4. `/frontend/src/components/posts/CreatePostForm.vue` - **новый компонент**

### 🔧 Затраченные токены
Приблизительно 70,000 токенов

### ✨ Следующие шаги

Рекомендуется:
1. Протестировать поиск на работающем сервере
2. Протестировать создание постов с медиа
3. Добавить эмодзи пикер в форму создания поста
4. Добавить выбор видимости поста (public/private)
